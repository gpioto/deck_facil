<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Deck Fácil | Redefinir Senha</title>
	<meta content='width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no' name='viewport'>
	<link href="https://maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css" rel="stylesheet" type="text/css" />
	<%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => true %>
	<%= csrf_meta_tags %>
</head>
<body class="login-page">
	<div class="login-box">
		<div class="login-logo">
			<a href="/"><b>Deck</b>Fácil</a>
		</div>
		<div class="login-box-body">
			<p class="login-box-msg">Digite uma carta</p>
			<%= form_for(:card, :url => {:controller => 'test', :action => 'test_byName'}) do |f| %>
				<fieldset>
			    	<div class="form-group">
			    		<div class ="description">Carta:</div><div class ="field"><%= f.text_field :card%> </div>
			    	</div>
			    	<%= f.submit "Enviar" %>
				</fieldset>
			<% end %>
			
			<p class="login-box-msg">Digite um ID de carta</p>
			<%= form_for(:card, :url => {:controller => 'test', :action => 'test_byID'}) do |f| %>
				<fieldset>
			    	<div class="form-group">
			    		<div class ="description">Carta:</div><div class ="field"><%= f.text_field :card%> </div>
			    	</div>
			    	<%= f.submit "Enviar" %>
				</fieldset>
			<% end %>
			<% flash.each do |name, msg| %>
                  <%= content_tag :div, msg, class: name %>
            <% end %>
            <% if @user && @user.errors.any? %>
              <ul class="Signup_Errors">
              <% for message_error in @user.errors.full_messages %>
                  <li>* <%= message_error %></li>
              <% end %>
              </ul>
            <% end %>
		</div>
	</div>
<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</body>
</html>